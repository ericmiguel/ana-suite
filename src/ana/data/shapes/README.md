# ANA Region Contours

These files are the Brazilian UF contour bundled by the original `pluvia`
prototype. The layer contains 27 federal units in `EPSG:4674`; the `acronym`
field is used by `ana.Region` to select one state. `Region.BRAZIL` uses the
union of all features.
